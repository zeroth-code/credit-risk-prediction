# Streamlit Community Cloud does not read pyproject.toml or uv.lock, so the deployed
# runtime is pinned here. Local development and CI use uv with uv.lock instead.
#
# Versions are exported from uv.lock and must stay identical to it: the release model is
# a joblib pickle, so scikit-learn and lightgbm must match the training versions.
# Regenerate with scripts/export_requirements.sh after changing dependencies.
joblib==1.5.3
lightgbm==4.7.0
numpy==2.4.6
pandas==3.0.5
plotly==6.9.0
pydantic==2.13.4
pyyaml==6.0.3
scikit-learn==1.9.0
scipy==1.18.0
streamlit==1.59.1
